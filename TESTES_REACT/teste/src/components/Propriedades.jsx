const somatoria = (num) => {
    result = calculo+num

}

const Propriedades = ({calculo}) => {

  return (

    <div>
        <h3>O valor é: {(e) => somatoria(3)}</h3>
      
    </div>

  )
}

export default Propriedades